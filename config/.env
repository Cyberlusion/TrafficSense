# An optional file where environment variables are stored (especially useful for sensitive data like API keys, database passwords, etc.). These values can be loaded by the application at runtime.

# .env
APP_ENV=development
DATABASE_URL=postgresql://user:password@localhost/mydb
API_KEY=my-secret-api-key
